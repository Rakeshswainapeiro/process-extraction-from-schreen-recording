services:
  app:
    build: .
    container_name: process-extractor-pro
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped

volumes:
  app_data:
